module.exports = {
  testEnvironment: 'node',
  testMatch: [
    '**/__tests__/**/*.js',
    '**/?(*.)+(spec|test).js'
  ],
  // Optional: You can add setup files here if needed
  // setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
};
